# 组合逻辑芯片日志（组合逻辑代理专属）

> 每次 git 提交时在末尾追加一条。不要改动 `log-sequential-chips.md`（那是时序代理的日志）。
>
> 条目格式：
> `## 日期 · 组合逻辑` + 新增芯片型号与一句话功能 + 测试结果（全量通过数）。

## 2026-08-16

### 批次 1

新增 74LS10（三路 3 输入与非门）、74LS11（三路 3 输入与门）、74LS20（双路 4 输入与非门，NC 脚 3/11 未建模）；`SignalState` 增加 nand3/and3/nor3/nand4/and4 运算。全量测试 162/162 通过。

### 批次 2

新增 74LS21（双路 4 输入与门，NC 脚 3/11 未建模）、74LS27（三路 3 输入或非门）、74LS42（BCD→十进制译码器，非法码 10–15 全部输出高）。全量测试 162/162 通过。

### 批次 3

新增 74LS138（3-8 线译码器/多路分配器，G1/~G2A/~G2B 三使能）、74LS139（双 2-4 线译码器，各自低有效使能）、74LS151（8 选 1 数据选择器，Y/~W 互补输出）。定向测试 27/27 通过；全量测试待批次 4 完成后一并验证。

### 批次 4

新增 74LS153（双 4 选 1 数据选择器，共用地址 A/B、独立选通）、74LS157（四 2 选 1 数据选择器，共用 S/~E、非反相输出）、74LS283（4 位二进制全加器，C0/C4 进位）。另把芯片手册分组提示从「各门电路行为一致」改为「各组逻辑行为一致」，并同步 `test/widgets/chip_manual_test.dart` 断言。定向测试 27/27 通过，全量测试 284/284 通过。

### 批次 5

新增 74LS85（4 位数值比较器，MSB 优先比较、三路级联输入/输出）、74LS148（8-3 线优先编码器，低有效 I/O、EI/EO/GS 级联）、74LS154（4-16 线译码器，DIP-24、双低有效使能）。定向测试 27/27 通过；全量测试待 `chip_factory.dart` 注册完成后一并验证。

### 批次 6

新增 74LS125（四三态总线缓冲器，各路独立低有效使能）、74LS244（八三态缓冲器，两组四路）、74LS245（八总线收发器，DIR 方向 + ~OE 使能，双向三态；因仿真器无 inout 引脚类型，A/B 两侧均建模为输入型引脚）。定向测试 27/27 通过；全量测试待 `chip_factory.dart` 注册完成后一并验证。
