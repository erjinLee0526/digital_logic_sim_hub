# 时序逻辑芯片日志（时序逻辑代理专属）

> 每次 git 提交时在末尾追加一条。不要改动 `log-combinational-chips.md`（那是组合代理的日志）。
>
> 条目格式：
> `## 日期 · 时序` + 新增芯片型号与一句话功能 + 测试结果（全量通过数）。

## 2026-08-16

- **74LS175**：四路 D 触发器，公共上升沿时钟与异步清零。
- **74LS273**：八路 D 触发器，公共上升沿时钟与异步清零。
- **74LS373**：八位透明锁存器，LE 锁存、OE 三态输出。
- 全量 `flutter test`：162/162 通过。

## 2026-08-16（第二批）

- **74LS164**：8 位串入并出移位寄存器，A·B 串行输入、上升沿移位、异步清零。
- **74LS90**：十进制计数器，2 分频 + 5 分频两段，异步清零与置 9（NC 脚 4/13 未建模）。
- **74LS161**：4 位同步二进制计数器，异步清零、同步置数、计数/保持与 RCO 进位输出。
- 全量 `flutter test`：184/184 通过。

## 2026-08-16（第三批）

- **74LS93**：4 位二进制计数器，CKA÷2 + CKB÷8 两段、异步清零（NC 脚 4/6/7/13 未建模）。
- **74LS160**：十进制同步计数器，异步清零、同步置数、ENT/ENP 使能，状态 9 时 RCO 有效。
- **74LS163**：4 位同步二进制计数器，清零与置数均同步，计数到 15 时 RCO 有效。
- 全量 `flutter test`：210/210 通过。

## 2026-08-16（第四批）

- **74LS393**：双 4 位二进制计数器，两组独立计数、各自 MR 异步清零。
- **74LS165**：8 位并入串出移位寄存器，~PL 异步置数、CLK/CLK INH 或逻辑移位，QH 与 ~QH 输出。
- **74LS194**：4 位双向通用移位寄存器，S0/S1 选择保持、右移、左移、并行置数，~MR 异步清零。
- 全量 `flutter test`：257/257 通过。

## 2026-08-16（第五批）

- **74LS76**：双 JK 触发器，下降沿（高到低）触发，各自异步置位/清零；注意 VCC=5、GND=13 的非常规 DIP 布局。
- **74LS112**：双 JK 触发器，下降沿触发，各自异步置位/清零。
- **74LS174**：六 D 触发器，公共上升沿时钟 CP 与异步清零 ~MR。
- 全量 `flutter test`：357/357 通过。

## 2026-08-16（第六批）

- **74LS190**：十进制（BCD 8421）同步加/减计数器，单一 CP 上升沿计数，~PL 异步置数、~CE 使能、U/D 选方向，输出 TC/RC。
- **74LS191**：4 位二进制同步加/减计数器，单一 CP 上升沿计数，~PL 异步置数、~CE 使能、U/D 选方向，输出 TC/RC。
- **74LS193**：4 位二进制加/减计数器，独立 CPU/CPD 双时钟，MR 高电平异步清零、~PL 异步置数，输出 TCU/TCD。
- 全量 `flutter test`：379/379 通过。

## 2026-08-16（第七批）

- **74LS192**：十进制（BCD 8421）加/减计数器，CPU/CPD 双时钟，MR 高电平异步清零、~PL 异步置数，输出 TCU/TCD。
- **74LS195**：4 位并行存取移位寄存器，~MR 异步清零，~PE 选并行置数/右移，J/~K（低有效 K）串行输入，输出 Q0~Q3 与 ~Q3。
- **74LS374**：八路上升沿触发的 D 触发器，公共时钟 CP 与三态输出使能 ~OE。
- 全量 `flutter test`：448/448 通过。
